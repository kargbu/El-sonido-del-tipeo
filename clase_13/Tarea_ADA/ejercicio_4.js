/* Crea una función declarada llamada par que reciba un número y devuelva "El número es par" 
o "El número es impar". Declara una variable local para guardar el resultado.
*/
const prompt = require('prompt-sync')();

let num_usuario = parseFloat(prompt('Ingresa un número:
    '))

function par_o_impar () {

}