// Programa el área de un triángulo

function area_triangulo(base, altura) {
    let area = (base * altura) / 2;
    return area; // Devuelve el valor calculado
}

console.log(area_triangulo(3, 5)); // Llamada a la función con base 3 y altura