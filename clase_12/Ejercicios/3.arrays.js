// Arrays son matrices. La maestra dice que es un conjunto de elementos o datos de distinto nativos //
const prompt = require('prompt-sync')()

let colores = ['rojo', 'verde', 'azul', 'violeta'];
let numeros = [23, 56, 78, 90];
let booleanos = [true, false, true, true];
let mezcla = ['Karina', 26, true, colores];
// Imprimir un arreglo 
console.log(mezcla);

// Acceder a un dato en específico dentro de los arreglos
console.log(mezcla[1]);

// Un arreglo dentro de un arreglo
console.log(mezcla[3][0]);

/* Agregar Elementos: push(), unshift()

Eliminar Elementos: pop(), shift()

Encontrar Elementos: indexOf(), includes()

Iterar sobre una Matriz: forEach(), map()*/

